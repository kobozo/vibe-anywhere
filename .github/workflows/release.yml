name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 1.0.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    name: Build and Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      - name: Prepare release package
        run: |
          # Create release directory
          mkdir -p release/session-hub

          # Copy standalone build
          cp -r .next/standalone/* release/session-hub/

          # Copy static files to correct location
          mkdir -p release/session-hub/.next/static
          cp -r .next/static/* release/session-hub/.next/static/

          # Copy public folder
          if [ -d "public" ]; then
            cp -r public release/session-hub/
          fi

          # Copy docker files
          cp -r docker release/session-hub/

          # Copy scripts
          cp -r scripts release/session-hub/

          # Copy configuration templates
          cp .env.example release/session-hub/

          # Copy database files for migrations
          cp -r drizzle release/session-hub/ 2>/dev/null || true
          cp drizzle.config.ts release/session-hub/ 2>/dev/null || true

          # Copy package.json for scripts
          cp package.json release/session-hub/

          # Create version file
          echo "${{ inputs.version }}" > release/session-hub/VERSION

          # Create tarball
          cd release
          tar -czvf session-hub-v${{ inputs.version }}.tar.gz session-hub

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ inputs.version }}
          name: Session Hub v${{ inputs.version }}
          body: |
            ## Session Hub v${{ inputs.version }}

            ### Installation

            ```bash
            curl -fsSL https://raw.githubusercontent.com/${{ github.repository }}/main/scripts/install.sh | sudo bash
            ```

            ### What's Changed

            See the [commit history](https://github.com/${{ github.repository }}/commits/v${{ inputs.version }}) for details.
          files: |
            release/session-hub-v${{ inputs.version }}.tar.gz
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
