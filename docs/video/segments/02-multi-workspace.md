# Segment 2: Multi-Workspace Overview

**Duration:** 10 seconds

---

## First Frame (Screenshot Description)

Dashboard sidebar expanded and in focus. Three repositories visible with multiple workspaces nested under each:

**Repository 1: "api-backend"**
- workspace: main (green running indicator)
- workspace: feature/auth (green running indicator)
- workspace: bugfix/login (yellow starting indicator)

**Repository 2: "frontend-app"**
- workspace: develop (green running indicator)
- workspace: feature/dashboard (green running indicator)

**Repository 3: "shared-libs"**
- workspace: main (green running indicator)

The currently selected workspace is highlighted with a subtle background color. Branch names visible next to each workspace in smaller text.

### Key Visual Elements
- Expandable repository tree
- Nested workspace items
- Color-coded status indicators (green/yellow)
- Branch name labels
- Selection highlight
- Workspace count badges on repositories

---

## Video Content (10 seconds)

**0-2 sec**: Mouse cursor visible, moves to first workspace "api-backend/feature/auth".

**2-4 sec**: Click on workspace. Main terminal view instantly switches, showing different code/terminal content.

**4-6 sec**: Cursor moves to "frontend-app/develop", clicks. Another instant switch to different terminal state.

**6-8 sec**: Cursor moves to "shared-libs/main", clicks. Terminal shows yet another isolated environment.

**8-10 sec**: Text overlay fades in: "Parallel isolated sessions. Zero conflicts."

### Motion & Effects
- Natural mouse cursor movement
- Click highlight animation on workspace items
- Instant (but smooth) terminal content transitions
- Each terminal shows visibly different content
- Text fade-in (0.5 sec)

---

## Production Checklist

- [ ] Three repositories configured with varied workspaces
- [ ] Each workspace has unique terminal content
- [ ] Mouse cursor recording or animation
- [ ] Click highlight effect
- [ ] Text overlay: "Parallel isolated sessions. Zero conflicts."
