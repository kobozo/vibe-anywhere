{
  "name": "@session-hub/agent",
  "version": "1.7.0",
  "description": "Session Hub sidecar agent for LXC containers",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "bundle": "npm run build && tar -czf agent-bundle.tar.gz dist package.json package-lock.json"
  },
  "dependencies": {
    "simple-git": "^3.27.0",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
