{
  "name": "@vibe-anywhere/agent",
  "version": "3.1.3",
  "description": "Vibe Anywhere sidecar agent for LXC containers",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "bundle:code": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/agent-bundled.js --external:node:* --minify --sourcemap",
    "bundle:binary": "bash scripts/build-binary.sh",
    "bundle:cli": "cd ../vibe-anywhere-cli && npm install && npm run build && mkdir -p ../agent/cli && cp dist/vibe-anywhere ../agent/cli/vibe-anywhere && chmod +x ../agent/cli/vibe-anywhere",
    "bundle": "npm run bundle:binary && npm run bundle:cli && mkdir -p bundle-temp && cp dist/vibe-anywhere-agent bundle-temp/ && cp cli/vibe-anywhere bundle-temp/ && tar -czf agent-bundle.tar.gz -C bundle-temp . && rm -rf bundle-temp"
  },
  "dependencies": {
    "simple-git": "^3.27.0",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.25.0",
    "postject": "^1.0.0-alpha.6",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
